Hooks folder (app/hooks)

- Place shared React hooks here. Keep hooks generic and reusable.
- Export hooks from `app/hooks/index.js` for consistency.

Example hooks to include:
- `useToggle.js` - boolean toggle helper
- `useUsername.js` - form state + simple validation
